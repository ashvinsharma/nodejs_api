const IMAGE_SRC = './images';
const IMAGE_DEST = './public';
const COLLECTION_TOKENS = 'tokens';

/**
 * @desc This file contains all the constants used in the project except the ones
 * which may contain sensitive, such constants are stored in .env file.
 * @type {{IMAGE_SRC: string, IMAGE_DEST: string, COLLECTION_TOKENS: string}}
 */
module.exports = {
  IMAGE_SRC,
  IMAGE_DEST,
  COLLECTION_TOKENS,
};
